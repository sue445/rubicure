name: Publish gem to rubygems.org

on:
  workflow_dispatch:

jobs:
  release:
    uses: sue445/workflows/.github/workflows/release_gem.yml@main
    with:
      repo_name: sue445/rubicure
      gem_name:  rubicure
    secrets:
      gh_token: ${{ secrets.GITHUB_TOKEN }}
